import React from 'react';
import classes from './GamesTiles.module.css';

const GamesTiles: React.FC = () => {
    return (
        <div className={classes.GamesTilesContainer}>

        </div>
    );
};

export default GamesTiles;
